# Home Assistant Secrets
# Managed by Ansible - do not commit to git
# Source: Ansible Vault

# Slack webhooks
slack_log_webhook: "https://hooks.slack.com/services/{{ vault_logging_token }}"
slack_alert_webhook: "https://hooks.slack.com/services/{{ vault_alert_token }}"

# Home Assistant API token (long-lived access token)
ha_monitor_token: "{{ vault_ha_monitor_token }}"

# NS API key for train notifications
ns_api_key: "{{ vault_ns_api_key }}"
