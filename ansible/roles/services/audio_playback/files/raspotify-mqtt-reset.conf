# Managed by Ansible — reset the retained MQTT playback state when raspotify
# (re)starts, so HA never sees a stale "playing" after a reboot or crash.
# The "-" prefix makes the reset best-effort: a broker blip won't block startup.
[Service]
ExecStartPre=-/usr/local/bin/spotify_event.sh reset
