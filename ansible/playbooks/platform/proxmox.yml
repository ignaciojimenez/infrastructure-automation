---
# Proxmox host-specific configuration
# Monitoring cron jobs are deployed by the platform/proxmox role via deploy_monitoring.yml.

- name: Proxmox platform configuration
  hosts: proxmox_hosts
  gather_facts: false
  tasks:
    - name: Proxmox platform acknowledged
      ansible.builtin.debug:
        msg: "Proxmox monitoring deployed separately via deploy_monitoring.yml"
      tags: [proxmox, platform]
