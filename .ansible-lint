---
exclude_paths:
  - .git/
  - migration_backup*/
  - tmp/
  
skip_list:
  - yaml[line-length]
  - no-changed-when
  
warn_list:
  - experimental
  - command-instead-of-shell
  
use_default_rules: true

# parseable, quiet, strict are CLI flags — not valid in config file
